{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'cd \"${CLAUDE_PROJECT_DIR:-$PWD}\" && (scripts/dev/setup-symlinks.sh --check || scripts/dev/setup-symlinks.sh)'",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
